// Create a number variable num with some value,
    //Now, print "good" if number is divisible by 10 and print "bad" if it's not.

let num = 40;

if (num % 10 === 0) {
    console.log("good");
} else {
    console.log("bad");
}